'''
A random variable, k , follows Poisson distribution with mean of miu. 
Find the probability with which the random variable k  is equal to x .
'''

import math

miu = float(input())
x = float(input())

poisson_prob = ((miu ** x) * math.exp(-miu)) / math.factorial(x)

print("%.3f" %poisson_prob)
